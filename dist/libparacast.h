/* Code generated by cmd/cgo; DO NOT EDIT. */

/* package main/cabi */


#line 1 "cgo-builtin-export-prolog"

#include <stddef.h>

#ifndef GO_CGO_EXPORT_PROLOGUE_H
#define GO_CGO_EXPORT_PROLOGUE_H

#ifndef GO_CGO_GOSTRING_TYPEDEF
typedef struct { const char *p; ptrdiff_t n; } _GoString_;
#endif

#endif

/* Start of preamble from import "C" comments.  */


#line 3 "cabi.go"

#include <stdint.h>
#include <stdlib.h>

#line 1 "cgo-generated-wrapper"


/* End of preamble from import "C" comments.  */


/* Start of boilerplate cgo prologue.  */
#line 1 "cgo-gcc-export-header-prolog"

#ifndef GO_CGO_PROLOGUE_H
#define GO_CGO_PROLOGUE_H

typedef signed char GoInt8;
typedef unsigned char GoUint8;
typedef short GoInt16;
typedef unsigned short GoUint16;
typedef int GoInt32;
typedef unsigned int GoUint32;
typedef long long GoInt64;
typedef unsigned long long GoUint64;
typedef GoInt64 GoInt;
typedef GoUint64 GoUint;
typedef size_t GoUintptr;
typedef float GoFloat32;
typedef double GoFloat64;
#ifdef _MSC_VER
#include <complex.h>
typedef _Fcomplex GoComplex64;
typedef _Dcomplex GoComplex128;
#else
typedef float _Complex GoComplex64;
typedef double _Complex GoComplex128;
#endif

/*
  static assertion to make sure the file is being used on architecture
  at least with matching size of GoInt.
*/
typedef char _check_for_64_bit_pointer_matching_GoInt[sizeof(void*)==64/8 ? 1:-1];

#ifndef GO_CGO_GOSTRING_TYPEDEF
typedef _GoString_ GoString;
#endif
typedef void *GoMap;
typedef void *GoChan;
typedef struct { void *t; void *v; } GoInterface;
typedef struct { void *data; GoInt len; GoInt cap; } GoSlice;

#endif

/* End of boilerplate cgo prologue.  */

#ifdef __cplusplus
extern "C" {
#endif

extern char* Paracast_GetLastError();

// Create a new network (float32) and return a handle (0 on failure).
// layers: int32 array of length 2*L: [w0,h0,w1,h1,...]
// activations: array of char* (L items), e.g. {"linear","relu","softmax"}
// fullyConnected: bytes (0/1) (L items) – input entry ignored; starting from layer1.
//
extern long long int Paracast_NewNetwork(void* layersPtr, int layersCount, char** actsPtr, int actsCount, unsigned char* fullPtr, int fullCount, int enableWebGPU);

// Create large network for dramatic GPU vs CPU testing
//
extern long long int Paracast_CreateLargeNetwork();

// Initialize GPU for a network
//
extern int Paracast_InitGPU(long long int h);

// Disable GPU for a network
//
extern void Paracast_DisableGPU(long long int h);

// Generate random test data
//
extern void Paracast_GenerateTestData(float* data, int numSamples, int seed);

// Run forward pass on a batch of samples
//
extern long long int Paracast_ForwardBatch(long long int h, float* inputData, int numSamples, float* outputData);

// Warm up GPU (run several forward passes)
//
extern long long int Paracast_WarmupGPU(long long int h, int warmupSamples);

// Compare two output arrays and return max difference
//
extern float Paracast_CompareOutputs(float* output1, float* output2, int numElements);

// Load a model from JSON string; returns handle (0 on failure).
//
extern long long int Paracast_LoadFromJSON(char* jsonStr, int enableWebGPU);

// Save a model to JSON file path (returns 1 on success, 0 on failure).
//
extern int Paracast_SaveJSON(long long int h, char* path);

// Forward pass on a single sample (returns 1 on success, 0 on failure).
// `input` is row-major float32 of size (inH*inW).
//
extern int Paracast_Forward(long long int h, float* input, int inW, int inH);

// Read the output vector (flattened) into `out` (float32).
// Returns number of floats written (>0) or 0 on failure.
//
extern int Paracast_GetOutput(long long int h, float* out, int outMax);

// Train for N epochs on (inputs, targets).
// Returns 1 on success, 0 on failure.
// Inputs/targets are packed batches of samples back-to-back (row-major each).
// dims: inW,inH,outW,outH. batch is number of samples provided.
// lr is learning rate. If clipUpper<clipLower you get no clipping.
//
extern int Paracast_Train(long long int h, float* inputs, int inW, int inH, float* targets, int outW, int outH, int batch, int epochs, float lr, int useGPU, float clipUpper, float clipLower);

// Destroy and free a network (also cleans up GPU if active).
//
extern void Paracast_Free(long long int h);

#ifdef __cplusplus
}
#endif
